<div
  id="delete-schedule-modal"
  class="fixed inset-0 z-50 hidden bg-black/50 backdrop-blur-sm"
>
  <div class="flex items-center justify-center min-h-screen">
    <div class="bg-white dark:bg-zinc-800 rounded-lg p-6 w-full max-w-md mx-4">
      <h3 class="text-lg font-bold mb-4 dark:text-white">Confirm Delete</h3>
      <p class="dark:text-gray-300 mb-6">
        Are you sure you want to delete <span class="font-bold" id="delete-job-name"></span> scheduled message? This action
        cannot be undone.
      </p>
      <div class="flex justify-end space-x-2">
        <button
          type="button"
          id="close-delete-modal"
          class="px-4 py-2 text-gray-600 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-zinc-700 rounded-lg"
        >
          Cancel
        </button>
        <button
          type="button"
          id="confirm-delete-schedule"
          class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg"
        >
          Delete
        </button>
      </div>
    </div>
  </div>
</div>
